# Pending work — pick up here

## GoXMR Pay (branch: feat/goxmr-pay)
Skeleton committed but not deployed. Files in:
- server/pay/schema.js     (3 tables)
- server/pay/auth.js       (API keys + webhook HMAC)
- server/pay/routes.js     (REST + dashboard + embed serve)
- server/pay/embed.js      (the public pay.js shim)
- server/pay/paymentScanner.js (expire loop + webhook retry queue)

Wired into server/index.js. Backend modules parse, not yet:
- Frontend pages for money.goxmr.click (landing + dashboard + checkout)
- Subdomain detection ("money" reserved sub in App.tsx)
- Webhook scanner needs per-merchant view-key spinning (Phase 2)
- Public docs page at money.goxmr.click/docs

Decisions already made by user:
- 0% forever, funded by donations
- Separate accounts in money.goxmr.click (not goxmr.click reuse)
- Hosted-first, self-host in Phase 3
- MIT licensed
- Federated: self-hosters opt-in to feed main directory

When resuming: switch to feat/goxmr-pay branch, build money landing page first.
