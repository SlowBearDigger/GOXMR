# CPANEL DNS API Configuration (V1.15+)
# Create token in: cPanel -> Security -> API Tokens
# Required permissions: DNS (Zone Editor)

CPANEL_API_TOKEN=your_token_here
CPANEL_USERNAME=your_cpanel_username
CPANEL_HOST=goxmr.click

# Optional: Only if your cPanel uses a custom port or non-standard URL
# CPANEL_BASE_URL=https://goxmr.click:2083
